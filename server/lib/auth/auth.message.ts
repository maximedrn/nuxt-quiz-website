/** Static messages for the auth domain. */
export const enum AuthMessage {
  CODE_TAKEN = 'This code is already in use — choose another.',
  INVALID_CREDENTIALS = 'Invalid code.',
  INVALID_FORMAT = 'Code must be exactly 8 digits.',
}
