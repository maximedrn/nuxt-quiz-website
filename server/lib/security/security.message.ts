/** Static messages for the security domain. */
export const enum SecurityMessage {
  CONSUME_FAILED = 'Rate-limit check failed',
  REDIS_URL_MISSING = 'REDIS_URL is required to initialise the security service',
}
