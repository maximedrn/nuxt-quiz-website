/** Static messages for the storage domain. */
export const enum StorageMessage {
  QUERY_FAILED = 'Storage query failed',
  SESSION_INSERT_FAILED = 'Failed to create session — no row returned.',
  NOT_IMPLEMENTED = 'Storage operation not wired to the deployed contract yet.',
}
